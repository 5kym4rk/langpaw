# Sao chép thành .env.import.local (KHÔNG commit — đã nằm trong .gitignore).
# Chỉ dùng cho script importer chạy trên máy/CI, KHÔNG đưa vào frontend.

# Korean Basic Dictionary (krdict) Open API key:
# Đăng ký: https://krdict.korean.go.kr/eng/openApi/openApiRegister
KRDICT_API_KEY=
