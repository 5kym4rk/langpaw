# LangPaw environment variables (MVP does not require any at runtime).
# Data-import scripts may use API keys locally or via CI secrets ONLY.
# Never put keys in frontend code or commit real values.

# Example: Korean Basic Dictionary import key (used only by scripts/import-korean.ts)
# KRDICT_API_KEY=
